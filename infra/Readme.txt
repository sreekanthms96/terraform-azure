1. after completing the terraform code use the "loadenv-powershell" 
2. command to load env file to powershell before "terraform apply"
3. whenever we close the shell, use this command in new terminal
